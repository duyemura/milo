import type { InspectOpts, SiteReport } from "./types.ts";

// Implemented in Task 10. Stub keeps the barrel + tsc happy during T0–T9.
export async function inspectSite(_opts: InspectOpts): Promise<SiteReport> {
  throw new Error("inspectSite not yet implemented");
}
