import type { SiteReport } from "./types.ts";

// Implemented in Task 11. Stub keeps the barrel + tsc happy during T0–T10.
export function renderSiteReport(_report: SiteReport): string {
  throw new Error("renderSiteReport not yet implemented");
}
