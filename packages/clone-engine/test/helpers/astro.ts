/**
 * Shared Astro-finding helpers for tests.
 * Imports from src/astro.ts (the engine's own install) — no stale spike paths here.
 */
export { findAstroModules, findAstroJs } from "../../src/astro.ts";
