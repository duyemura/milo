---
const { brand, nav } = Astro.props;
---

<header class="nav">
  <div class="nav-inner">
    <a href="/" class="logo">
      <svg width="30" height="30" viewBox="0 0 24 24" fill="none" aria-hidden="true">
        <path d="M13 2 L4.5 13.5 H11 L9.5 22 L19.5 9.5 H12.5 L13 2 Z" fill="var(--accent)" />
      </svg>
      <span>{brand.name}</span>
    </a>
    <button class="hamburger" aria-label="Menu" aria-expanded="false">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#000" stroke-width="2.4" stroke-linecap="round">
        <path d="M3 6 H21 M3 12 H21 M3 18 H21" />
      </svg>
    </button>
    <nav class="links">
      {
        nav.programs.length > 0 && (
          <div class="dropdown">
            <button class="dropdown-toggle" aria-haspopup="true">
              Programs
              <svg width="11" height="11" viewBox="0 0 12 12" aria-hidden="true">
                <path d="M2.5 4.5 L6 8 L9.5 4.5" stroke="currentColor" stroke-width="1.8" fill="none" stroke-linecap="round" />
              </svg>
            </button>
            <div class="dropdown-list">
              {nav.programs.map((p) => (
                <a href={p.href}>{p.label}</a>
              ))}
            </div>
          </div>
        )
      }
      {nav.links.map((l) => <a href={l.href}>{l.label}</a>)}
      <a href={nav.cta.href} class="nav-cta">{nav.cta.label}</a>
    </nav>
  </div>
</header>

<script>
  const burger = document.querySelector(".hamburger");
  const navEl = document.querySelector(".nav");
  burger?.addEventListener("click", () => {
    const open = navEl!.classList.toggle("open");
    burger.setAttribute("aria-expanded", String(open));
  });
  document.querySelector(".dropdown-toggle")?.addEventListener("click", (e) => {
    if (window.matchMedia("(max-width: 900px)").matches) {
      (e.currentTarget as HTMLElement).closest(".dropdown")?.classList.toggle("open");
    }
  });
</script>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 10;
    background: #fff;
    color: #000;
  }
  .nav-inner {
    max-width: 1400px;
    margin: 0 auto;
    padding: 14px 40px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .logo {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 19px;
    font-weight: 900;
    font-style: italic;
    letter-spacing: 0;
    text-transform: uppercase;
  }
  .hamburger {
    display: none;
    background: none;
    border: 0;
    padding: 8px;
    cursor: pointer;
  }
  .links {
    display: flex;
    align-items: center;
    gap: 30px;
    font-size: 13.5px;
    font-weight: 700;
    letter-spacing: 0.3px;
    text-transform: uppercase;
  }
  .links > a:hover {
    color: var(--accent);
  }
  .dropdown {
    position: relative;
  }
  .dropdown-toggle {
    display: flex;
    align-items: center;
    gap: 6px;
    font: inherit;
    text-transform: inherit;
    color: inherit;
    background: none;
    border: 0;
    cursor: pointer;
    padding: 10px 0;
  }
  .dropdown-list {
    position: absolute;
    top: 100%;
    left: -18px;
    min-width: 220px;
    background: #fff;
    padding: 14px 0;
    box-shadow: 0 14px 30px rgba(0, 0, 0, 0.14);
    display: none;
    flex-direction: column;
  }
  .dropdown:hover .dropdown-list,
  .dropdown:focus-within .dropdown-list {
    display: flex;
  }
  .dropdown-list a {
    font-size: 13.5px;
    font-weight: 700;
    padding: 10px 20px;
    white-space: nowrap;
  }
  .dropdown-list a:hover {
    color: var(--accent);
  }
  .nav-cta {
    background: var(--accent);
    color: #fff;
    padding: 13px 26px;
  }
  .nav-cta:hover {
    color: #fff;
    filter: brightness(0.9);
  }

  @media (max-width: 900px) {
    .nav-inner {
      padding: 12px 20px;
    }
    .hamburger {
      display: block;
    }
    .links {
      display: none;
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background: #fff;
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      padding: 8px 0 26px;
      box-shadow: 0 30px 40px rgba(0, 0, 0, 0.18);
    }
    .nav.open .links {
      display: flex;
    }
    .links > a,
    .dropdown-toggle {
      padding: 16px 20px;
      width: 100%;
      text-align: left;
    }
    .dropdown-list {
      position: static;
      box-shadow: none;
      padding: 0 0 8px;
    }
    .dropdown:hover .dropdown-list {
      display: none;
    }
    .dropdown.open .dropdown-list,
    .dropdown.open:hover .dropdown-list {
      display: flex;
    }
    .dropdown-list a {
      padding: 12px 36px;
    }
    .nav-cta {
      margin: 10px 20px 0;
      text-align: center;
    }
  }
</style>
